@page
@model PRN232_FA25_Assignment_G7.RazorPages.Pages.Moderator.Violations.IndexModel
@{
    ViewData["Title"] = "Violation Management";
}

<h1>@ViewData["Title"]</h1>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

@if (TempData["AuthError"] != null)
{
    <div class="alert alert-warning">@TempData["AuthError"]</div>
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

<!-- Search and Filter -->
<div class="card mb-4">
    <div class="card-body">
        <form method="get" class="row g-3">
            <div class="col-md-6">
                <label for="SearchTerm" class="form-label">Search</label>
                <input type="text" class="form-control" id="SearchTerm" name="SearchTerm"
                       value="@Model.SearchTerm" placeholder="Student ID, submission ID, examiner...">
            </div>
            <div class="col-md-4">
                <label for="StatusFilter" class="form-label">Status</label>
                <select class="form-select" id="StatusFilter" name="StatusFilter">
                    <option value="">All Statuses</option>
                    <option value="Pending" selected="@(Model.StatusFilter == "Pending")">Pending</option>
                    <option value="Approved" selected="@(Model.StatusFilter == "Approved")">Approved</option>
                    <option value="Rejected" selected="@(Model.StatusFilter == "Rejected")">Rejected</option>
                    <option value="Escalated" selected="@(Model.StatusFilter == "Escalated")">Escalated</option>
                </select>
            </div>
            <div class="col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-primary me-2">Search</button>
                <a href="@Url.Page("./Index")" class="btn btn-outline-secondary">Clear</a>
            </div>
        </form>
    </div>
</div>

@if (Model.Violations != null && Model.Violations.Any())
{
    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">Violations Requiring Review</h5>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Violation ID</th>
                            <th>Submission ID</th>
                            <th>Reported By</th>
                            <th>Type</th>
                            <th>Status</th>
                            <th>Reported At</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var violation in Model.Violations)
                        {
                            <tr>
                                <td>@violation.Id</td>
                                <td>@violation.SubmissionId</td>
                                <td>@violation.ExaminerName</td>
                                <td>
                                    <span class="badge bg-secondary">@violation.Type</span>
                                </td>
                                <td>
                                    <span class="badge bg-@(violation.Status == "Pending" ? "warning" : violation.Status == "Approved" ? "success" : violation.Status == "Rejected" ? "danger" : "info")">
                                        @violation.Status
                                    </span>
                                </td>
                                <td>@violation.ReportedAt.ToString("yyyy-MM-dd HH:mm")</td>
                                <td>
                                    @if (violation.Status == "Pending")
                                    {
                                        <a asp-page="./Review" asp-route-id="@violation.Id" class="btn btn-sm btn-outline-primary">
                                            Review
                                        </a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Reviewed</span>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <div class="alert alert-info">
        @if (!string.IsNullOrEmpty(Model.SearchTerm) || !string.IsNullOrEmpty(Model.StatusFilter))
        {
            <text>No violations found matching your search criteria.</text>
        }
        else
        {
            <text>No violations available for review.</text>
        }
    </div>
}
